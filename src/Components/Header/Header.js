// Dependencies
import React from 'react';
import { NavLink } from 'react-router-dom';

// CSS
import './Header.css';

export default function Header() {
    return (
        <header>
            Header
        </header>
    );
}